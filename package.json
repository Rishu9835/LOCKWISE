{
  "name": "robotics-club-door-lock",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && cd frontend && npm install && npm run build",
    "vercel-build": "npm install && cd frontend && npm install && npm run build"
  },
  "keywords": ["door-lock", "robotics", "iot", "access-control"],
  "author": "Robotics Club NIT Allahabad",
  "license": "MIT",
  "description": "Robotics Club Door Lock Management System with OTP authentication and Google Sheets integration",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "googleapis": "^159.0.0",
    "node-schedule": "^2.1.1",
    "nodemailer": "^7.0.5",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
